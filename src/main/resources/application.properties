spring.application.name=todolist
server.port=8082

# Serve the frontend from the classpath /static folder (default) and
# avoid caching during development so UI changes show immediately.
spring.web.resources.cache.period=0
